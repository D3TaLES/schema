{
  "$id": "https://raw.githubusercontent.com/D3TaLES/schema/main/schema_backend/ml.schema.json",
  "title": "ml",
  "description": "Machine learning on redox active molecules",
  "type": "object",

  "properties": {
    "_id": {
      "description": "A unique identifier for the data hashed from identifying properties",
      "type": "string"
    },
    "mol_id": {
        "description": "A unique identifier for the molecule",
        "type": "string"
    },
    "submission_info": {
      "description": "Information about who/when submitted the data",
      "type": "object",
      "$ref": "https://raw.githubusercontent.com/D3TaLES/schema/main/schema_backend/submission.schema.json"
    }

  },
  "additionalProperties": false,
  "required": ["_id", "mol_id"]
}