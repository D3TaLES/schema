{
  "$id": "https://raw.githubusercontent.com/D3TaLES/schema/reb_dev/schema_backend/computation.schema.json",
  "title": "computation",
  "description": "The structure information derived from computation for a molecule entry in database",
  "type": "object",
  "properties": {
    "_id": {
      "description": "A unique identifier for the data hashed from identifying properties",
      "type": "string"
    },
    "mol_id": {
      "description": "A unique identifier for the molecule",
      "type": "string"
    },
    "mol_info": {
      "description": "The basic structural information derivable from the molecule's SMILES structure",
      "$ref": "https://raw.githubusercontent.com/D3TaLES/schema/reb_dev/schema_frontend/mol_info.schema.json"
    },
    "submission_info": {
      "description": "Info about who submitted the data and how",
      "$ref": "https://raw.githubusercontent.com/D3TaLES/schema/reb_dev/schema_backend/submission.schema.json"
    },
    "calculation_type": {
      "type": "string",
      "description": "Type of calculation file opt_cation, neutral_cation, etc."
    },
    "runtime": {
      "description": "The job runtime in core hours",
      "type": "number"
    },
    "data": {
      "oneOf": [
        {"$ref": "https://raw.githubusercontent.com/D3TaLES/schema/reb_dev/schema_backend/computation.dft.schema.json"}
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "_id",
    "mol_id"
  ]
}